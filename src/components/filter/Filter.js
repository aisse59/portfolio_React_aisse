import React from "react";
import { Link } from "react-router-dom";


const Filter = () => {
  return (
    <div>
      <button>
        <Link to="/langages">Texte du bouton</Link>
      </button>
    </div>
  );
};

export default Filter;
